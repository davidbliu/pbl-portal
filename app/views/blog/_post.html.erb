
	<h2 id = '<%= post.id %>'><%= post.title %></h2>
	<% if post.tags %>
	<% post.tags.each do |tag| %>
		<div class = 'label-default label'><%= tag %></div>
	<% end %>
	<% end %>
	<div style = 'clear:both;height:20px;'></div>
	<div><%= raw(post.content)%></div>
