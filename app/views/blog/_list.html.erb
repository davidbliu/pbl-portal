
  <% posts.each do |post| %>
  <tr class = 'post-toggle' id = '<%= post.id %>'>
    <td><%= date_string(post.created_at) %></td>
    <td><%= post.title %></td>
    <td><%= post.author %></td>
    <td><%= group_string(post.groups) %></td>
  </tr>
  <% end %>

